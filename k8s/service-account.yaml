apiVersion: v1
kind: ServiceAccount
metadata:
  name: agw-app
  namespace: artisan-gem-works
  annotations:
    # IRSA: grants this pod identity access to AWS Secrets Manager and ECR
    eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/agw-app-role
