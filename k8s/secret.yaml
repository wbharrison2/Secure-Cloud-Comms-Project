# This Secret is managed by External Secrets Operator.
# Do NOT edit manually — it is synced from AWS Secrets Manager every hour.
# See external-secrets/external-secret.yaml for the sync configuration.
#
# Keys populated automatically: JWT_SECRET, DATABASE_URL, REDIS_URL, STRIPE_SECRET_KEY
apiVersion: v1
kind: Secret
metadata:
  name: agw-secrets
  namespace: artisan-gem-works
  annotations:
    managed-by: external-secrets-operator
type: Opaque
